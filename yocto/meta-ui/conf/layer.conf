BBPATH =. "${LAYERDIR}:"
BBFILES += "${LAYERDIR}/recipes*/*.bb \
	    ${LAYERDIR}/recipes*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-ui"
BBFILE_PATTERN_meta-ui = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-ui = "7"
LAYERSERIES_COMPAT_meta-ui = "kirkstone"
